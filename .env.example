# Gmail API Configuration
# Download your OAuth2 credentials from Google Cloud Console
# and save as client_secret_*.json in the project root

# Optional: Specify custom paths for credentials and tokens
# CREDENTIALS_FILE=path/to/client_secret.json
# TOKEN_FILE=path/to/token.json

# LLM API Configuration (for future phases)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here
